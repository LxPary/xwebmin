#ACPolicy       = deny
#ACAllowIP      = 127.0.0.1
#ACBlockMessage = Sorry, access is only allowed from trusted hosts

# Uncomment the following to allow access from a local network
#ACAllowIP      = 10.0.0.*
#ACAllowIP      = 192.168.0.*
AuthRealm = realm name (will be shown as info text in most browsers)

# use the apache compatible auth scheme
AuthRequire = HTPASSWD

# the login and password pairs are
# kept in this file:
AuthHtpasswdFilename = /usr/local/webgui/.htpasswd 
